
function squareOfWordLength(arguments){
    var length = arguments.length
    var squareOfLength =length * length;
    console.log(`The Total Length Square JavaScript of  is:  ${squareOfLength}`);
}
squareOfWordLength("JavaScript")


function squareOfWordLength(arguments){
    var length = arguments.length
    var squareOfLength =length * length;
    console.log(`The Total Length Square Google Chrome of  is:  ${squareOfLength}`);
}
squareOfWordLength("Google Chrome")

function squareOfWordLength(arguments){
    var length = arguments.length
    var squareOfLength =length * length;
    console.log(`The Total Length Square Developer Smart of  is:  ${squareOfLength}`);
}
squareOfWordLength("Developer Smart")

console.log(`==========Step 2=========`);
function sentence(){
    var angular = "I am Angular Developer"
    var length = angular.length;
    var totalWord = angular.split(" ")
    console.log(totalWord);
    var Result = length/4;
    
    console.log(`Division after String length divide by total words is:${Result}`);  
    var Multiplication = length*4;                                          
   console.log(`Multiplication after String length multiplied by total no.of words is:${Multiplication}`);
}
sentence()